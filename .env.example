# ==========================================
# mini-SMTP-server Configuration
# ==========================================

# Your domain name
DOMAIN=example.com

# DKIM selector (must match the selector used when generating keys)
DKIM_SELECTOR=mail

# SMTP server hostname (usually smtp.yourdomain.com)
HOSTNAME=smtp.example.com

# ==========================================
# Optional Settings
# ==========================================

# Maximum message size in bytes (default: 10MB)
# MESSAGE_SIZE_LIMIT=10485760

# Allowed sender domains (comma-separated, default: uses DOMAIN)
# ALLOWED_SENDER_DOMAINS=example.com,another-domain.com

# Timezone (default: UTC)
# TZ=Europe/Warsaw

# ==========================================
# Instructions
# ==========================================
# 1. Copy this file to .env
# 2. Update DOMAIN with your actual domain
# 3. Generate DKIM keys: ./generate-dkim-keys.ps1 (Windows) or ./generate-dkim-keys.sh (Linux/Mac)
# 4. Add DNS TXT record shown by the generator
# 5. Run: docker-compose up -d
